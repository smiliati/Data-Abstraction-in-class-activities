import java.util.HashMap;


public class sumStuff {
    
    //given an array of ints and a target value, return the two indeces of the two numbers in the array that would sum to the target
    public int[] twoSum(int[] nums, int target) {
        for (int i = 0; i<nums.length; i++) {
            int diff = target - nums[i];
            if (numMap.containskey)
        }
    }

}
